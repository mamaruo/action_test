name: PR Size Labeler

on:
  pull_request:
    types:
      - opened
      - synchronize

jobs:
  label-pr-size:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install
             npm install @actions/github
             npm install @actions/core
      

      - name: Run PR Size Labeler
        run: node pr-size-labeler.js
        env:
          PR_SIZE: ${{ steps.setup.outputs.PR_SIZE }}

      - name: Create/Update PR Label
        uses: actions/github-script@v5
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const gh = require('@actions/github');
            const actionsCore = require('@actions/core');
      
            const prSize = process.env.PR_SIZE;
            let label = '';
      
            if (prSize <= 100) {
              label = 'small';
            } else if (prSize <= 1000) {
              label = 'medium';
            } else {
              label = 'large';
            }
      
            const octokit = gh.getOctokit(actionsCore.getInput('github-token'));
            octokit.issues.addLabels({
              owner: gh.context.repo.owner,
              repo: gh.context.repo.repo,
              issue_number: gh.context.issue.number,
              labels: [label]
            });